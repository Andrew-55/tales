import {COLORS} from '@app/assets/styles/constants';

export const THEMES = {
  dark: {
    upload: {
      backgroundColor: COLORS.color_500,
      borderColor: COLORS.color_400,
    },
    uploadText: {
      color: COLORS.color_200,
    },
  },
  light: {
    upload: {
      backgroundColor: COLORS.color_150,
      borderColor: COLORS.primary_default_dark_mode,
    },
    uploadText: {
      color: COLORS.color_700,
    },
  },
};
